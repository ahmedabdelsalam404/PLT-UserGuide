name: Convert Word Doc to PDF and SVGs

on:
  schedule:
    - cron: "0 6 * * *" # Every day at 6 AM UTC
  workflow_dispatch:     # Manual trigger

jobs:
  convert-doc:
    runs-on: ubuntu-latest

    steps:
    - name: 📥 Checkout Repo
      uses: actions/checkout@v3

    - name: 🛠 Install LibreOffice and Poppler
      run: |
        sudo apt-get update
        sudo apt-get install -y libreoffice poppler-utils

    - name: 🧾 Convert .docx to .pdf
      run: |
        libreoffice --headless --convert-to pdf "PLT-Metrics-Guide.docx" --outdir .

    - name: 📤 Convert .pdf to .svg pages
      run: |
        mkdir -p svg_pages
        pdftocairo -svg PLT-Metrics-Guide.pdf svg_pages/page

    - name: 🚀 Commit .pdf and .svg files
      env:
        TOKEN: ${{ secrets.GITHUB_TOKEN }}
      run: |
        git config user.name "github-actions"
        git config user.email "github-actions@github.com"

        # Stage everything first to avoid rebase failure
        git add -A

        # Authenticate and pull latest changes with rebase
        git remote set-url origin https://x-access-token:$TOKEN@github.com/${{ github.repository }}
        git pull origin main --rebase

        # Stage again after rebase (just in case)
        git add -A
        git commit -m "📄 Auto-convert Word doc to PDF and SVGs" || echo "Nothing to commit"
        git push origin HEAD:main
